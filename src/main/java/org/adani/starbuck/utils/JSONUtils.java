package org.adani.starbuck.utils;

public class JSONUtils {

    /**
     * Create a new object given a JSON configuration String.
     * @param jsonString
     *      The JSON configuration String.
     * @param instanceType
     *      The type to cast the created object.
     * @param <T>
     *     The type to cast the created object.
     * @return
     *  Object that is the representation of the JSON configuration.
     */
    public static <T> T createInstance(String jsonString, Class<T> instanceType)   {
        /**
         * TODO: Implement createInstance functionality
         */
        return null;
    }
}
