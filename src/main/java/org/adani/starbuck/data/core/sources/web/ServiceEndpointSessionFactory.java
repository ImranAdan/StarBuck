package org.adani.starbuck.data.core.sources.web;

import org.adani.starbuck.data.core.sources.queryable.QueryableInitialisationConfiguration;
import org.adani.starbuck.data.core.sources.queryable.QueryableSession;

public class ServiceEndpointSessionFactory {

    public static QueryableSession newInstance(QueryableInitialisationConfiguration queryableType) {

        //TODO: Create different startagies for SOAP and RESTFUL Endpoints

        return null;

    }
}
